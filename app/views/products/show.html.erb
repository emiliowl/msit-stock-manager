<p id="notice"><%= notice %></p>

<p> 
  <p>
    <%= cl_image_tag("#{@product.class.to_s << @product.id.to_s << @product.created_at.to_s}.png", :alt => "Product Image", :version => Time.now.to_i) %>
  </p>
  <b>Name:</b>
  <%= @product.name %>
</p>


<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
